/**
 * Legacy 기능 스위치 — 스펙 v1.1 §5.1.
 *
 * 원칙: **코드·라우트·DB 는 남기고 "동작"만 끈다.** 물리 삭제는 Phase 6 에서
 * 의존성 분석과 함께 한다(§24). 되돌리려면 값만 true 로 바꾸면 된다.
 *
 * 타입을 `boolean` 으로 명시하는 이유: 리터럴 `false` 로 좁혀지면 사용처가
 * "항상 거짓인 조건"으로 보여 린트·타입 경고가 난다.
 */

/**
 * 홈 방문 시 랭킹 점수를 백그라운드로 갱신할지.
 *
 * 랭킹은 §5 에서 legacy 로 내렸는데, 점수 갱신만 홈에 남아 계속 돌고 있었다
 * (`src/app/dashboard/page.tsx`). 아무도 못 보는 리더보드를 위해 드로다운·마일스톤·
 * 구성비중을 계산하고 `ranking_scores` 에 쓰던 작업이라 껐다.
 *
 * 끈 뒤에도:
 *  · `ranking_scores` 테이블과 기존 행은 그대로 둔다(삭제 금지, §23)
 *  · `/ranking` 을 직접 방문하면 그 페이지에서는 여전히 자기 점수를 갱신한다
 *  · 벤치마크(PME) 조회는 성과 카드가 쓰므로 이 스위치와 무관하게 계속 돈다
 */
export const LEGACY_RANKING_SYNC: boolean = false;
